import WorksCategoryPage, { generateStaticParams as baseGenerateStaticParams } from '@/app/[lang]/works/[slug]/page';

export const generateStaticParams = baseGenerateStaticParams;

export default WorksCategoryPage;
